import 'package:flutter/material.dart';

class FPrice extends StatelessWidget {
  const FPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
