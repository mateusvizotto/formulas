import 'package:flutter/material.dart';

class IPrice extends StatelessWidget {
  const IPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
