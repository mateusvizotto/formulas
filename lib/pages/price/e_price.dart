import 'package:flutter/material.dart';

class EPrice extends StatelessWidget {
  const EPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
