import 'package:flutter/material.dart';

class BPrice extends StatelessWidget {
  const BPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
