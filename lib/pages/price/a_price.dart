import 'package:flutter/material.dart';

class APrice extends StatelessWidget {
  const APrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
