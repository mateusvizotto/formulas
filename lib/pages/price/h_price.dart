import 'package:flutter/material.dart';

class HPrice extends StatelessWidget {
  const HPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
