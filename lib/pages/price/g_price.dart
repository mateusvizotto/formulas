import 'package:flutter/material.dart';

class GPrice extends StatelessWidget {
  const GPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
