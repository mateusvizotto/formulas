import 'package:flutter/material.dart';

class JPrice extends StatelessWidget {
  const JPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
