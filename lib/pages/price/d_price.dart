import 'package:flutter/material.dart';

class DPrice extends StatelessWidget {
  const DPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
