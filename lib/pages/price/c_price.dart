import 'package:flutter/material.dart';

class CPrice extends StatelessWidget {
  const CPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
