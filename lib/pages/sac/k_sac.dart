import 'package:flutter/material.dart';

class KSac extends StatelessWidget {
  const KSac({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
