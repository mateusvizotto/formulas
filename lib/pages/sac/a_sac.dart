import 'package:flutter/material.dart';

class ASac extends StatelessWidget {
  const ASac({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Teste'),
      ),
      body: const Center(
        child: Text('Teste'),
      ),
    );
  }
}

//TODO: Continuar criando a página
