import 'package:flutter/material.dart';

class GSac extends StatelessWidget {
  const GSac({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
