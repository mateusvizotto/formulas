import 'package:flutter/material.dart';

class BSac extends StatelessWidget {
  const BSac({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
