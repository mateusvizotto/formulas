import 'package:flutter/material.dart';

class ISac extends StatelessWidget {
  const ISac({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
