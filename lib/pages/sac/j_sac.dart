import 'package:flutter/material.dart';

class JSac extends StatelessWidget {
  const JSac({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
